/*
Ext.define ( 'extspect.object.rec.ClassManagerRec' ,
	{	extend : 'extspect.object.rec.BaseRec' ,
		properties : []
		// classes maps
		// createdListeners, namedCreatedListeners
	}
)*/

// Ext.ClassManager.classes lists most of the system classes you want
// in the Ext tab
// but not EventManager, Loader, Router, Viewport
// some are duplicates

/* WHAT WE DONT WANT
( property === "namespaceRewrites" ) || // Ext.ClassManager
( property === "namespaceParseCache" ) || // Ext.ClassManager
*/
